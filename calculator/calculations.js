



export default function calculate(val1, val2, operation) {
    if (operation === "Addition") {
        const answer = val1 + val2;
        return answer;
    }
    else if (operation === "Subtraction") {
        const answer = val1 - val2;
        return answer;
    }
}